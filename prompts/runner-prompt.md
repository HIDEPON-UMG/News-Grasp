本リポジトリ内の prompts/routine-system.md を Read で読み込み、その指示に厳密に従って当日（JST）の News-Grasp 日次 digest を生成してください。具体的には、watchlist の対象ジャンルに対して各ジャンル 10 件を WebSearch で収集し、過去 90 日の articles.jsonl と関連付けを行い、digest/YYYY-MM-DD-{Genre}.md と data/articles.jsonl 追記を作成してください。git commit / git push / docs 生成 / publish gate 実行は絶対に行わないでください。runner が gate、docs 生成、commit、push、retry budget、fallback publish を一元管理します。メール配信機能は 2026-06-05 に廃止済みなので、メール組み立て・送信ステップは実行しないでください。サムネ取得 (ステップ 3-B) は段階 1 (tools/fetch_ogp.py) を必ず最初に実行し、ng-thumb-common-{cat}.jpg を digest md に直接書き込んではいけません (2026-05-25 強化ルール)。検索後はカテゴリごとに data/search_audit/{YYYY-MM-DD}/{category_id}.json を必ず作成し、queries / raw_results_total / candidates_total / selected_total / coverage_terms_checked / dropped を記録してください。5件未満で確定するカテゴリは quality_shortfall_reason と search_audit の両方が無いと公開前 gate が落ちます。gate に失敗しそうな場合でも同じ工程を何度も繰り返さず、修正不能な理由を data/_status.md に記録して停止してください。
