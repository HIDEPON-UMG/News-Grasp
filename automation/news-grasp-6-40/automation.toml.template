version = 1
id = "news-grasp-6-40"
kind = "cron"
name = "News-Grasp 臨時本線日次バッチ 6:00 記事作成・公開"
status = "ACTIVE"
rrule = "RRULE:FREQ=DAILY;BYHOUR=6;BYMINUTE=0;BYSECOND=0"
model = "gpt-5.6-luna"
reasoning_effort = "max"
execution_environment = "local"
cwds = ['${NEWS_GRASP_REPO_ROOT}']

prompt = """
News-Graspの日次公開を実行してください。このautomation は監査バッチではありません。

最初に `$news-grasp-direct-mainline` を読み、Lunaの操作を一回の起動に限定してください。`automation/skills/news-grasp-e2e-discipline/SKILL.md` はruntime内部のRelease-only境界であり、日次中にLunaが個別実行しません。

最優先事項:
- 完全な品質で記事公開するまで完了してはならない。
- 対象日はAsia/Tokyoの当日、titleは `YY/MM/DD News-Grasp 臨時本線日次バッチ 6:00 記事作成・公開` とし、runtimeの `title_status`（例: `title_status=already_ok`）を報告してください。
- 目標は45分、SLO は 90 分です。75 分時点で新規 high-cost stage を開始しない。90 分を超えた場合は SLO 超過を記録し、実行可能な public-critical successor がある限り継続します。

実行:
- 次のcommandを一回だけ起動してください。
  `C:\\Users\\hidek\\AppData\\Local\\Programs\\Python\\Python312\\python.exe -m tools.news_grasp_direct_runtime daily`
- runtimeが `static_check` → `scoped_contract_unit` → `current_issue_integration` → `external_publication` → `consumer_public_verification` → `atomic_completion` を所有します。
- `tools.publish_inventory.scheduled_category_ids(issue_date)` が当日の対象カテゴリ正本です。
- 開始時seal、same-run復帰、成果物再利用、provider照合、public verifier、direct completion guardはruntimeが判断します。Lunaはrun ID、SHA、manifest ID、個別phase、retry、exact successorを操作しません。
- task contractの保護日再実行はstate作成・外部処理前に `protected_release_reexecution_forbidden` となります。

品質・回復:
- Summaryはfrontmatter付きMarkdown、DeepDiveはcurrent issueの実bytesを正本にします。
- reporter/editor/repair/newsroom_editorはLuna/max、DeepDiveはSol/highです。
- 開始後のID/SHA driftは現runを止めず次回readiness debtへ分離します。lease失効時は同じrunへCAS takeoverし、Green producer receiptと成果物を再実行しません。
- provider ACKが得られない場合は `unknown_unobtainable` を保持し、再送しません。
- 公開に直結しない事項は `post_publish_issue_list` に一行だけ残します。
- コスト起因またはハーネス起因で、記事品質・公開完成と無関係な停止判断をしない。

完了条件:
- 完全な品質で記事公開するまで完了してはならない。
- consumer-owned verifierと`atomic_completion`がWeb、Daily audio、DeepDive article/audio、YouTube、playlist、notification、distribution、publish-status、remote、Pagesを同一issue date/run lineageでGreenにした場合だけcompletedです。
- callerの自己申告、artifact存在、runner/readiness、URL 200、commit/push単独はcompletion authorityではない。

禁止:
- 完全な品質で記事公開するまで完了してはならない。
- 旧runner、個別operation CLI、raw script、手動repair、polling、同一command再実行、NoPublish、URL 200 単独、historical、Playwright、full test、原因調査を開始しません。
- `public incomplete を Green として最終応答しない`でください。安全なexact successorがあればruntimeが継続し、改ざん、DB破損、競合writer、許可外副作用だけを`failed_integrity`にします。

最終報告:
- 結論、状態、根拠、残るrisk、次の作業の順に、`issue_date`、`title_status`、public status、typed terminal、exact successor、主要surface、elapsed、SLO差分、`post_publish_issue_list`を報告してください。
- Git commit ID は観測値としてだけ扱い、制御authorityにしません。
"""
created_at = 1788014431516
updated_at = 1788577200000
